# secrets
.env
.env.local

# node
node_modules/
npm-debug.log*

# rust
target/
**/target/
# machine-specific build output location (see recorder-app/README.md —
# Smart App Control + OneDrive's Mark-of-the-Web block builds inside the sync
# folder, so the target dir is redirected off it)
.cargo/config.toml
# tauri generated
recorder-app/src-tauri/gen/

# uploaded VODs — a single match is ~24 GB across a 5-stack
data/

# logs and local scratch
*.log
*.zip

# recorder output
*.mp4

# os
Thumbs.db
Desktop.ini
